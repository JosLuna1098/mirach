# Mirach runtime dependencies.
# Pinned minimum versions that are known to work; newer is usually fine.

# Core
faster-whisper>=1.2,<2.0      # STT (uses CTranslate2 under the hood)
piper-tts>=1.4,<2.0           # TTS
sounddevice>=0.5,<1.0         # Microphone capture (PortAudio bindings)
numpy>=1.26,<3.0
ddgs>=9.0                     # DuckDuckGo search (used by web_search.py)

# CUDA runtime libraries — only needed if running Whisper on NVIDIA GPU.
# Comment these out for CPU-only setups (slower but works everywhere).
nvidia-cublas-cu12>=12.0
nvidia-cudnn-cu12>=9.0
